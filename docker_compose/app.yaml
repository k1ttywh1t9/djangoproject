services:
  main-app:
    container_name: main-app
    build:
      context: ..
      dockerfile: Dockerfile 
    ports:
      - "${DJANGO_PORT}:8000"
    command: "sh /entrypoint.sh"
    env_file:
      - ../.env
    depends_on:
      - postgres
    volumes:
      - ..:/app/